{
  "name": "docray",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd src && uvicorn server:app --reload --port 8000",
    "start": "cd src && uvicorn server:app --host 0.0.0.0 --port 8000",
    "hydradb:pipeline": "node scripts/hydradb_pipeline.mjs"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
